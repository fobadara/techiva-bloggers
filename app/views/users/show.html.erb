<%= render partial: 'shared/user', locals: { user: @user } %>

<article class="border pb-5 pt-3 px-3" >
  <h3 class="pb-2">Bio</h3>
  <p><%= @user.bio %></p>
</article>

<%= render partial: 'shared/post', collection: @posts, locals: { length: 40} %>
<%= link_to "See all posts", user_posts_path(@user.id), class: "btn btn-primary my-5" %>

